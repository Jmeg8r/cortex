# =============================================================================
# Cortex Runtime Dependencies
# =============================================================================
# These are the packages needed to run cortex.
#
# For development dependencies, see requirements-dev.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Tier 2: Vector Embeddings (optional - graceful degradation if not installed)
# -----------------------------------------------------------------------------
# sentence-transformers requires torch, which is large (~2GB with CUDA)
# For CPU-only (recommended for most users): pip install sentence-transformers
# The model itself is ~90MB and downloads on first use.
sentence-transformers>=5.6.1
numpy>=1.24.0

# sqlite-vec provides vector similarity search in SQLite
# Used for semantic search in hybrid search (FTS5 + vectors)
sqlite-vec>=0.1.9
